import { describe, it, expect, vi } from "vitest"; import { handleSystemMessage } from "../../../../web/static/shared/js/ws/message-handlers/system-messages.js"; vi.mock("../../../../web/static/shared/js/dom.js", () => ({ dom: { ssCacheSub: {} } })); describe("system-messages.js", () => { it("handles log message", () => { expect(() => handleSystemMessage({ type: "log", data: "test" })).not.toThrow(); }); });
