import { describe, it, expect, vi } from "vitest"; import { handleAuthMessage } from "../../../../web/static/shared/js/ws/message-handlers/auth-messages.js"; vi.mock("../../../../web/static/shared/js/dom.js", () => ({ dom: { adminSubmitBtn: {}, loginErrorMsg: {}, portalLoginForm: { classList: { add: vi.fn(), remove: vi.fn() } }, setupSubmitBtn: {}, setupErrorMsg: {}, setupConfirmErrorMsg: {}, setupScreen: { classList: { add: vi.fn(), remove: vi.fn() } }, portalScreen: { classList: { add: vi.fn(), remove: vi.fn() } }, adminUsername: {} } })); describe("auth-messages.js", () => { it("handles auth_status message", () => { expect(() => handleAuthMessage({ type: "auth_status", data: { success: false } })).not.toThrow(); }); });
