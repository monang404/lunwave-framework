# lunawave_framework.core.kernel -- generic runtime instrumentation and
# async helpers (observability metrics, memory stats, latency windows,
# server uptime clock, safe background-task creation). Moved verbatim from
# the app's core/ in Phase 2 (no music vocabulary found in Phase 0 audit).
