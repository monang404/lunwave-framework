# lunawave_framework.core -- generic kernel primitives (Phase 2 of the
# framework extraction). See docs/extraction/ in the app repo for the
# full plan. Domain-specific core code (state.py, events.py, commands.py,
# exceptions.py, ports.py, event_bus.py, command_bus.py) has NOT moved
# here yet -- that split is Phase 3, not this one.
